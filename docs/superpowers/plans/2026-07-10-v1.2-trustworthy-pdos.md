# DBand Studio v1.2 Implementation Record

## Delivered Work

- Add `PDOSCapabilities`, structured parser errors, and `DataLoader.inspect()`.
- Inspect DOSCAR, vasprun.xml, and VASPKIT PDOS before calculation.
- Recover only uniquely recognized field-less VASP 5/6 layouts.
- Attach source capabilities to parser metadata and result provenance.
- Replace hybridization zero filling with validated totals/components.
- Surface import status, diagnostics, and copyable issue details.
- Add compatibility, scientific-method, troubleshooting, CI, and release docs.

## Verification Commands

```powershell
$env:QT_QPA_PLATFORM='offscreen'
python -m pytest tests -q
python -m build
```

Real-data and installer checks remain manual because redistributable VASP 5/6
fixtures and an Inno Setup compiler are not bundled in the workspace.
